name: Java1

on:
  workflow_dispatch:

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-java@v2
        with:
          distribution: adopt
          java-version: 11
          set-java-home: false
      - name: run tests
        run: |
          java --version
